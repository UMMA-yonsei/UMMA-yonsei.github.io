# Videos

## 메인 배경 영상

`umma.mp4` 파일을 이 폴더에 넣으면 첫 화면 배경에서 자동 재생됩니다.
파일이 없으면 기존 YouTube 소개 영상이 배경으로 재생됩니다.

- 형식: MP4 (H.264), 소리 없음 (브라우저 자동재생은 무음일 때만 허용됨)
- 권장: 1920×1080, 10–30초 루프, 10MB 이하
- 파일명을 바꾸려면 `index.html`의 `data-hero-local-video` 안 `<source data-src>` 경로도 같이 수정
